# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Notes internes Claude (mémoire de session) — pas du code app
memory/

# Chantier migration (Prisma) — base locale de dev + secrets, jamais commités
# (.env.example reste suivi comme gabarit). Le schéma et les migrations LE sont.
.env
.env.*
!.env.example
*.db
*.db-journal

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel
.env*

# Fichiers d'import RGPD (donnees perso reelles) — JAMAIS commites (chantier import/export)
_import_local/
